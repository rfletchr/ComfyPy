[project]
name = "comfypy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "rfletchr", email = "13312808+rfletchr@users.noreply.github.com" }
]
requires-python = ">=3.12"
dependencies = ["websocket-client>=1.8"]

[project.scripts]
comfypy = "comfypy:main"

[build-system]
requires = ["uv_build>=0.11.16,<0.12.0"]
build-backend = "uv_build"
